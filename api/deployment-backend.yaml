apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: gcr.io/jenkinsgpc/jenkinsgpc-backend:v1
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "128Mi"  # Solicitar 128 megabytes de memoria
            cpu: "100m"      # Solicitar 100 milicore de CPU
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
