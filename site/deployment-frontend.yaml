apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: gcr.io/jenkinsgpc/jenkinsgpc-frontend:v1
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "256Mi"   # Establece la memoria solicitada
          limits:
            memory: "512Mi"   # Establece el límite de memoria
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
