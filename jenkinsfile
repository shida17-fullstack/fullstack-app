pipeline {
    agent any
    
    environment {
        // Obtener la rama real del repositorio de GitHub
        BRANCH_NAME = "${env.GIT_BRANCH}"
        GCP_SERVICE_ACCOUNT = '606820346362-compute@developer.gserviceaccount.com' // Definir la cuenta de servicio de GCP
    }
    
    stages {
        stage('Unit Tests') {
            steps {
                script {
                    // Ejecutar pruebas unitarias con Jest y SuperTest
                    sh 'npm install --prefix ./api'
                    sh 'npm run test --prefix ./api'
                    // Puedes agregar más pasos de prueba según sea necesario
                }
            }
        }
        
        stage('Cloud Build') {
            steps {
                // Utiliza withCredentials para obtener la credencial de GCP
                withCredentials([string(credentialsId: 'jenkinsgpc-service-account', variable: 'GCP_CREDENTIALS')]) {
                    // Ejemplo de uso del plugin de Cloud Build con credenciales
                    script {
                        googleCloudBuild(
                            credentialsId: 'jenkinsgpc-service-account',
                            request: file('cloudbuild.yaml'),
                            source: local('.'),
                            substitutions: "BRANCH_NAME=${BRANCH_NAME}",
                            credentials: 'GCP_CREDENTIALS' // Pasar las credenciales como parámetro
                        )
                    }
                }
            }
        }
    }
}
